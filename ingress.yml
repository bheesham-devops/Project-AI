apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: project-ai-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
  - host: project-ai.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: project-ai-service
            port:
              number: 80